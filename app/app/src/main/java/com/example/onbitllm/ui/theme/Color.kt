package com.example.onbitllm.ui.theme

import androidx.compose.ui.graphics.Color

// ダークテーマ: 青〜紫系カラーパレット
// 背景色
val BackgroundDeepDark = Color(0xFF0A0A1A)  // 最深背景
val BackgroundDark = Color(0xFF10102A)       // メイン背景
val BackgroundCard = Color(0xFF1A1A3A)       // カード背景
val BackgroundCardDark = Color(0xFF14142E)   // 濃いカード背景

// アクセントカラー
val PrimaryPurple = Color(0xFF7B5EA7)        // プライマリ紫
val PrimaryBlue = Color(0xFF4A6BFF)          // プライマリ青
val AccentCyan = Color(0xFF00E5D1)           // シアンアクセント
val AccentPurpleLight = Color(0xFFAA88FF)    // ライト紫アクセント

// 吹き出し色
val UserBubble = Color(0xFF2D2D6B)           // ユーザー吹き出し（青系）
val UserBubbleDark = Color(0xFF252562)       // ユーザー吹き出し濃い
val AiBubble = Color(0xFF1A1A35)             // AI吹き出し（ダーク）
val AiBubbleBorder = Color(0xFF2A2A55)       // AI吹き出しボーダー

// テキスト色
val TextPrimary = Color(0xFFEEEEFF)          // メインテキスト
val TextSecondary = Color(0xFFAAAAAA)        // サブテキスト
val TextMuted = Color(0xFF666688)            // ミュートテキスト
val TextAccent = Color(0xFF88AAFF)           // アクセントテキスト（応答時間等）

// 入力欄
val InputBackground = Color(0xFF1A1A3A)      // 入力欄背景
val InputBorder = Color(0xFF2A2A55)          // 入力欄ボーダー

// ボタン
val SendButtonBg = Color(0xFF5B4FE8)         // 送信ボタン
val SendButtonDisabled = Color(0xFF333366)   // 送信ボタン無効時

// ヘッダー
val HeaderBackground = Color(0xFF0D0D22)     // ヘッダー背景

// リソースモニター
val ResourceBarModel = Color(0xFF7B5EA7)     // モデルRAMバー
val ResourceBarSystem = Color(0xFF9B5BAF)    // システムRAMバー
val ResourceBarCpu = Color(0xFF00E5D1)       // CPUバー
val OverlayBackground = Color(0xCC000010)    // オーバーレイ背景
